# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
backend/.env

.claude/
.agent-browser/
.playwright-mcp/
deno.lock

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Database files (AppSchool.db is committed + shipped in the image; Railway `up`
# respects .gitignore, so ignoring *.db would drop it from the deploy → empty prod DB)
*.db-shm
*.db-wal

MIGRATION_GUIDE-0.9.8.md
SKATEBOARD_UI_EXTENSIONS_PROPOSAL.md

# Bun lockfile (package-lock.json is source of truth)
bun.lock

# Stray DB created when the test suite runs from the repo root
/databases/

# Build & tooling artifacts
coverage
backend/target
